# Alpamayo 1.5 on Jetson AGX Thor — DRAM 대역폭 실측 분석 보고서

**보고 날짜**: 2026-06-11  
**실험 환경**: Jetson AGX Thor (GB10B, SM 11.0, LPDDR5X 231 GB/s, 128 GB 공유 메모리)  
**모델**: Alpamayo 1.5 (Cosmos Reason2 8.2B + Action Expert 2.3B, BF16)

---

## 0. 핵심 요약

| 단계 | DRAM BW | 커널 간 유휴 | 상태 |
|------|---------|------------|------|
| VE | **35%** (80 GB/s) | 미측정 | BW 여유 65% — 추후 분석 |
| LM Prefill | **55%** (126 GB/s) | FlashAttention 구간 60% 여유 | BW 여유, DMA 삽입 가능 |
| LM Decode | **89%** (205 GB/s) | **10.6% (159 ms)** 커널 간 유휴 | BW 포화, 유휴는 커널 사이에서만 |
| Flow Matching | **88%** (203 GB/s) | 미측정 | BW 포화 — 추후 분석 |

**Fig. 1** 참조.

---

## 1. 측정 방법론

### 1.1 측정 목적

GPU가 언제, 어디서 노는지 파악하는 것이 목적이다. 모델 가중치는 **DRAM**(메인 메모리, Thor에서는 LPDDR5X 규격, 이론 최대 전송 속도 231 GB/s)에 저장되어 있고, GPU가 연산을 할 때마다 이 가중치를 DRAM에서 읽어와야 한다. 이 전송 속도가 얼마나 차 있는지를 측정하면 GPU가 연산 중인지, 아니면 데이터를 기다리며 노는지를 파악할 수 있다.

구체적으로 두 가지 질문에 답한다:
- 각 추론 단계(VE, Prefill, Decode, Flow)가 DRAM을 얼마나 쓰는지
- 단계 내부에서 어떤 커널이 병목인지, 그 사이에 유휴가 있는지

### 1.2 측정 도구

**ncu** (NVIDIA Nsight Compute): GPU 커널 하나하나에 대해 DRAM 전송량, 실행 시간 등 하드웨어 카운터를 직접 읽는 도구. 소프트웨어 추정치가 아닌 칩 내부 하드웨어 레벨의 실측값이다.

**nsys** (NVIDIA Nsight Systems): 시스템 전체 타임라인을 기록하는 도구. 코드 안에 **NVTX** 마커(예: `range_push("LM_Prefill")`)를 삽입하면 nsys가 해당 구간의 시작/끝 시간을 자동으로 기록한다. 이 덕분에 전체 추론 중 어느 부분이 VE인지, Prefill인지 정확히 구분해서 측정할 수 있다.

### 1.3 측정 방법 및 정확성 근거

**두 계층으로 측정**:

```
[계층 1] Stage-level  →  nsys NVTX 타임스탬프 + ncu 총량 교차 검증
                          각 단계의 총 DRAM 전송량과 소요 시간 파악

[계층 2] Kernel-level →  ncu --replay-mode kernel
                          단계 내부 커널 하나하나의 순간 DRAM BW 측정
                          현재 완료: LM Prefill, LM Decode
                          미측정:   VE, Flow Matching (진행 예정)
```

**핵심 측정 지표**: `lts__d_sectors_fill_sysmem.sum`

GPU L2 캐시에서 실제 LPDDR5X로 전송된 **sector** 수를 하드웨어가 직접 센 값이다. sector는 GPU가 DRAM에 접근하는 최소 단위로 **128바이트**에 해당한다. `sector 수 × 128 bytes = 실제 DRAM 전송량`이 된다. 이 값은 샘플링 없이 모든 전송을 카운트하므로 정확하다.

단, ncu는 정확한 측정을 위해 각 커널을 **2번 실행**(2-pass replay)한다. 하드웨어 카운터 슬롯이 제한적이라 메트릭을 두 묶음으로 나눠 커널을 두 번 돌리며 각각 다른 항목을 수집하는 방식이다. 그 결과 CSV에는 커널당 2행이 기록되어 값이 2배가 되므로, 전체 수치에 **÷2 보정**을 적용했다.

```
커널 A 1번째 실행 → DRAM sector 카운터 수집  ┐
커널 A 2번째 실행 → 실행 시간 카운터 수집    ┘ → CSV에 2줄 → ÷2 필요
```

**SM 활용률 측정 시도와 의문**:

DRAM BW와 함께 **SM**(Streaming Multiprocessor, GPU의 연산 코어) 활용률도 측정을 시도했다. 그런데 측정하면서 의문이 생겼다 — *커널 단위로 SM 활용률을 보는 것이 과연 우리가 원하는 정보를 주는가?* SM 활용률 카운터는 SM이 DRAM 응답을 기다리며 대기하는 중에도 "활성"으로 기록되기 때문에, SM이 바쁘다고 해서 실제로 유용한 연산을 하고 있다고 볼 수 없다. 결국 **"DRAM BW%가 얼마나 차 있는가"가 GPU 유휴를 판단하는 더 직접적인 지표**라는 판단으로 DRAM BW를 핵심 측정 지표로 확정했다.

---

## 2. 4단계 DRAM 대역폭 실측 결과

아래 표에서 **Peak%** 는 실제 측정된 DRAM 전송 속도를 이론 최대치(231 GB/s)로 나눈 비율이다. 89%라면 231 × 0.89 ≈ 205 GB/s로 DRAM이 거의 꽉 차 있다는 뜻이고, 35%라면 여유가 65% 남아 있다는 뜻이다. **L2 hit**은 메모리 요청 중 GPU 칩 내장 L2 캐시(32 MB, DRAM보다 수십 배 빠름)에서 해결된 비율로, 높을수록 DRAM 실제 접근이 줄어든다.

| 단계 | 시간 (ms) | DRAM Read | DRAM Write | **총 DRAM** | Read BW | **Peak%** | L2 hit |
|------|----------|-----------|------------|------------|---------|----------|--------|
| **VE** | 728 | 58.4 GB | 39.7 GB | 98.1 GB | 80 GB/s | **35%** | 49.2% |
| **LM Prefill** | 1,423 | 179.9 GB | 52.1 GB | 232.0 GB | 126 GB/s | **55%** | 29.6% |
| **LM Decode** | 1,503¹ | 307.5 GB | 15.9 GB | 323.4 GB | 205 GB/s | **89%** | 37.7% |
| **Flow Matching** | 870 | 176.7 GB | 67.6 GB | 244.3 GB | 203 GB/s | **88%** | 22.0% |
| **합계** | **4,524** | 722.5 GB | 175.2 GB | **897.7 GB** | — | — | — |

> ¹ 19 steps × 79.1 ms/step (AppendOnlyCache-C: KV 캐시 최적화 구현체, 2026-05-31 실측 확정)  
> Peak% = Read BW / 231 GB/s × 100

단계별로 두 가지 체제가 명확히 갈린다:

- **BW 여유 구간** — VE (35%), LM Prefill (55%): DRAM이 놀고 있는 시간 존재 → 이 여유 BW에 DMA를 끼워 넣어 다음 데이터를 미리 가져오는 것이 가능
- **BW 포화 구간** — LM Decode (89%), Flow Matching (88%): DRAM이 이미 거의 꽉 참 → DMA를 추가로 삽입할 여지 없음, 다른 전략 필요

---

## 3. Kernel-level 상세 분석

> ⚠ 현재 kernel-level 측정은 **LM Prefill, LM Decode** 완료. **VE, Flow Matching은 측정 진행 중**.

### 3.1 LM Prefill

Prefill은 입력 이미지+텍스트 전체(seq_len=3,086 토큰)를 한 번에 처리하는 단계다. 이때 가중치 행렬과 입력 행렬을 곱하는 **GEMM**(General Matrix Multiplication, 행렬×행렬 연산)이 주로 실행된다.

| 커널 그룹 | 호출 수 | DRAM BW% | 병목 분류 |
|----------|---------|----------|---------|
| 가중치 GEMM (nvjet_256x128) | 108 | ~70% | DRAM-bound |
| **FlashAttention** | 18 | **~39.5%** | **SRAM/compute-bound** |
| norm 등 기타 | 수백 | <10% | compute-bound |

**FlashAttention**은 일반 Attention 연산을 GPU 칩 내부의 고속 메모리(SRAM)에서 타일 단위로 처리해 DRAM 접근을 최소화하는 알고리즘이다. seq_len이 길수록 이점이 커지는데, Prefill(seq=3,086)에서 DRAM BW가 39.5%밖에 안 나오는 이유가 바로 이것이다. FlashAttention이 Prefill 전체 시간의 **52% (738 ms)** 를 차지하면서도 DRAM을 39.5%만 쓰기 때문에 — **이 41 ms 구간 동안 140 GB/s가 놀고 있다**. DMA가 들어갈 수 있는 구간이다.

### 3.2 LM Decode

Decode는 토큰을 한 개씩 순차 생성하는 단계로, 매번 입력이 단 1개의 토큰(seq_len=1)이다. 이 경우 GEMM이 **GEMV**(General Matrix-Vector Multiplication, 행렬×벡터 연산)로 축소된다. GEMV는 연산량 대비 DRAM에서 읽어야 하는 데이터가 압도적으로 많아(가중치 전체를 읽지만 계산은 벡터 하나분만) DRAM이 89%까지 포화된다.

| 구분 | 비율 | 내용 |
|------|------|------|
| GEMV 실행 | ~89.4% | DRAM 89% 포화 상태 |
| **커널 간 유휴** | **~10.6%** | GPU가 완전히 쉬는 구간 (≈ 159 ms) |

커널 간 유휴(159 ms)는 CPU가 다음 커널을 GPU에 제출(launch)하는 짧은 대기 시간(dispatch overhead)이 매 step마다 반복되면서 쌓인 것이다. **CUDA Graph**를 적용하면 커널들을 미리 하나의 그래프로 묶어 두고 한 번에 제출하므로 이 CPU dispatch 단계 자체가 사라져 유휴 시간을 제거할 수 있다.

### 3.3 VE / Flow Matching (kernel-level 측정 예정)

Stage-level에서 VE 35%, Flow Matching 88%로 확인됐다. 커널 내부에서 정확히 어디서 유휴가 발생하는지는 아직 측정하지 않았다. 측정 완료 후 §4 Bubble 지도를 업데이트할 예정이다.

---

## 4. Bubble 지도 — 각 자원의 유휴 시간

**Fig. 3** 참조 (VE/Flow 측정 완료 후 업데이트 예정).

현재까지 확인된 유휴 구간:

| 유휴 위치 | 크기 | 성격 | 제거 방법 |
|----------|------|------|---------|
| Prefill — FlashAttention 중 DRAM 여유 | 140 GB/s × 41 ms × 18회 | DRAM 대역폭 유휴 | cudaMemPrefetchAsync (DMA 동시 실행) |
| Decode — 커널 간 간격 | **159 ms** (10.6%) | GPU 완전 유휴 | CUDA Graph |
| VE — 어디서 유휴인지 | 미정 | 미측정 | 측정 후 확인 |
| Flow Matching — 어디서 유휴인지 | 미정 | 미측정 | 측정 후 확인 |

**DMA** (Direct Memory Access)는 SM을 거치지 않고 메모리 간 데이터를 직접 전송하는 전용 엔진이다. `cudaMemPrefetchAsync`를 사용하면 SM이 현재 레이어를 연산하는 동안 DMA가 독립적으로 다음 레이어 가중치를 미리 가져올 수 있어, 대기 시간이 연산 시간 뒤에 숨겨진다.

---

## 5. 결론 및 다음 실험 계획

| 우선순위 | 실험 | 기대 효과 |
|---------|------|---------|
| ★ 1 | VE / Flow Matching kernel-level 측정 | 전체 유휴 구간 완성, Bubble 지도 업데이트 |
| ★ 2 | LM Prefill — cudaMemPrefetchAsync layer prefetch 구현 | Prefill ~-180 ms |
| ★ 3 | LM Decode — CUDA Graph 적용 (커널 간 유휴 제거) | Decode ~-159 ms |

---

## 첨부 Figure

| Figure | 파일 | 내용 |
|--------|------|------|
| **Fig. 1** | `figures/fig1_stage_bw.png` | 4단계 DRAM BW 포화도 |
| Fig. 2 | `figures/fig2_prefill_kernel.png` | Prefill 커널별 BW% |
| **Fig. 3** | `figures/fig3_bubble_map.png` | Bubble 지도 (VE/Flow 측정 후 업데이트 예정) |
| Fig. 4 | `figures/fig4_layer_feasibility.png` | Layer prefetch 실현 가능성 비교 |

*측정 데이터: `profiling_results/260609_ncu_full/`, `profiling_results/260611_prefill_bw/`*
